"""Слой доступа к данным (Repository layer).

Каждый репозиторий изолирует SQL-запросы для одной сущности.
Сервисы не должны содержать прямых вызовов SQLAlchemy — только методы репозитория.

Правила:
- Репозиторий не содержит бизнес-логики.
- Репозиторий не знает про HTTP, схемы Pydantic, workflow.
- Репозиторий возвращает ORM-объекты или None/list.
- Все методы асинхронные (async/await).
"""
